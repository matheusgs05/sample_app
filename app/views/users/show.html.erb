<% @classe = UserGroup.find_by(id: @user.idgrupo) %>

<% provide(:title, @user.name) %>
<div class="row">
  <aside class="span4">
    <section>
      <h1>
        <%= gravatar_for @user %>
        <h1> <%= @classe.nome %> </h1>
        <%= @user.name %> 
      </h1>
    </section>
  </aside>
</div>